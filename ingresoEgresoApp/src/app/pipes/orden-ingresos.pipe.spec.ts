import { OrdenIngresosPipe } from './orden-ingresos.pipe';

describe('OrdenIngresosPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenIngresosPipe();
    expect(pipe).toBeTruthy();
  });
});
